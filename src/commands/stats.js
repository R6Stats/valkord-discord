import * from '../utilities'

class StatsCommand {

  constructor ({ message, args: { username, platform, queue=null }}) {
    this._message = message
    this._username = username
    this._platform = utili
    this._queue = queue
  }

}
